#include<stdio.h>

/**
 * main - Entry point
 *
 * Description: Output of a string
 *
 * Return: 0(Success)
 */

int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);

	return (0);

}
