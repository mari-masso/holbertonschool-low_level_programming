#include "main.h"

/**
 * _strlen - entry point
 *
 * @s: the string to be measured in length
 * Description: 'Write a function that
 * returns the length of a string.'
 *
 * Return: 0(Success)
 */

int _strlen(char *s)
{
	char a;

	while ((*s = a) != '\0')
	{
		a++;
	}
	return (a);
}
