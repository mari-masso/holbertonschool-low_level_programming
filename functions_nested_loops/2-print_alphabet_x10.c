#include <main.h>

/**
 * main - Entry point
 *
 * Description: ' print the alphabet
 * once per line, 10 times, in lowercase.'
 *
 * Return: 0(Success)
 */

int main(void)
{
	for (x = 'a'; <= 'z'; x++)
	{
		putchar(x);
			putchar('\n');
	}
	return (0);
}
